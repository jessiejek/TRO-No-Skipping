# Required for /host login and GET /api/rsvp
HOST_PASSWORD=change-me-to-a-strong-secret

# Optional — when set, RSVPs persist to Vercel Blob instead of data/rsvps.json
# Create a Blob store in the Vercel dashboard and paste the read/write token.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
